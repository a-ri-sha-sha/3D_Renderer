#include "Renderer.h"

Picture renderer::Renderer::make(const World& world, size_t camera_id) {
    return Picture(); /*implementation*/
}

Picture renderer::Renderer::make_triangle(const World& world, size_t camera_id, const Triangle& triangle) {
    return Picture(); /*implementation*/
}

void renderer::Renderer::clipping(const World& world, size_t camera_id, const Triangle& triangle) {
    /*implementation*/
}
