#pragma once 

#include "Picture.h"

namespace renderer {
class Drawer {
public:
    void draw(const Picture& picture);
    // метод должен что-то возвращать, но я пока не понимаю что
private:
    // ???
};
} // namespace renderer